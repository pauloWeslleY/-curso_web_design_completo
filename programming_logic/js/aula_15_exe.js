let width = prompt('Typing a width!');
width = parseFloat(width);

let height = prompt('Typing a height');
height = parseFloat(height);

function showArea(vw, hw) {
   let calc = (vw * hw);
   alert(`Area all: ${calc}`);
}
